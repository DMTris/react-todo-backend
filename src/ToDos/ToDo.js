//create the ToDo class
/**
**@param id
**@param task
**@param isComplete
**/
export default class {
  constructor(id, task, isComplete){
    this.id = id;
    this.task = task;
    this.isComplete = isComplete
  }
}
